name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Vendor sing-box
        run: ./Scripts/vendor-singbox.sh
      - name: Build
        run: swift build
      - name: Test
        run: swift test
